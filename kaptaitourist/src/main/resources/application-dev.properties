# .env file import
spring.config.import=optional:file:.env[.properties]

# ── R2DBC (reactive) — transaction pooler port 6543 ──────────
spring.r2dbc.url=${SPRING.R2DBC.URL}
spring.r2dbc.username=${SPRING.R2DBC.USERNAME}
spring.r2dbc.password=${SPRING.R2DBC.PASSWORD}

# ── Liquibase (JDBC) — session pooler port 5432 ──────────────
spring.liquibase.url=${SPRING.LIQUIBASE.URL}
spring.liquibase.user=${SPRING.LIQUIBASE.USER}
spring.liquibase.password=${SPRING.LIQUIBASE.PASSWORD}
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml

# ── Logging ──────────────────────────────────────────────────
logging.level.org.springframework.data.r2dbc=DEBUG

# ── Supabase Storage ──────────────────────────────────────────
supabase.url=${SUPABASE.URL}
supabase.anon-key=${SUPABASE.ANON.KEY}
supabase.storage.bucket=${SUPABASE.STORAGE.BUCKET}

# Multipart - hard limit at framework level before reaches to handler
spring.webflux.multipart.max-file-size=4MB
spring.webflux.multipart.max-request-size=20MB

# ── CORS (comma-separated browser origins) ───────────────────
app.cors.allowed-origins=${APP.CORS.ALLOWED_ORIGINS:http://localhost:3000}

# ── Auth / JWT (HS256 secret must be >= 32 chars) ─────────────
app.jwt.secret=${APP.JWT.SECRET:dev-secret-change-me-in-prod-min-32-chars-long-please}
app.jwt.expiration-ms=${APP.JWT.EXPIRATION_MS:86400000}
# Bootstrap admin (created on startup if missing)
app.admin.email=${APP.ADMIN.EMAIL:admin@kaptai.local}
app.admin.password=${APP.ADMIN.PASSWORD:Admin@12345}
app.admin.name=${APP.ADMIN.NAME:Administrator}
app.admin.mobile=${APP.ADMIN.MOBILE:+8800000000000}



#spring.r2dbc.url=r2dbc:postgresql://localhost:5432/postgres
#spring.r2dbc.username=test
#spring.r2dbc.password=test
#spring.r2dbc.properties.schema=kaptai
#
## Liquibase config for JDBC migrations  ? separate JDBC URL
#spring.liquibase.enabled=true
#spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
#spring.liquibase.url=jdbc:postgresql://localhost:5432/postgres
#spring.liquibase.user=postgres
#spring.liquibase.password=test
